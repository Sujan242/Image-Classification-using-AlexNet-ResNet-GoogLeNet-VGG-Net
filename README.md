# Image-Classification-using-AlexNet-ResNet-GoogLeNet-VGG-Net

To run any file: 
1) open the file with google colab 
2) Change the runtime type to GPU accelerator
3) click ctrl+F9 to run all the files
